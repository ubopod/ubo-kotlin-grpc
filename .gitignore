# Gradle
.gradle/
.kotlin/
build/
out/

# IDE
.idea/
*.iml
.vscode/
.DS_Store

# Proto sources are mirrored from ../ubo_app/rpc/proto/ by
# generate-protos.sh and are NOT committed — same convention as the core
# repo, which gitignores /ubo_app/rpc/proto/ubo and its generated bindings.
# Regenerate with `uv run poe proto:kotlin` from the ubo-apple-apps root
# before building; the Gradle protobuf plugin turns these into Kotlin/Java
# stubs at build time.
protos/src/main/proto/
local.properties
captures/
